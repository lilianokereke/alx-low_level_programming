#include "main.h"
/**
 *
 * print_alphabet_x10 - Entry point
 */
void main(void)
	char alpha;
	int num = 0;
	{
	while (num++ < 9)
{
	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar(alpha);
	}
	{
		_putchar('\n');
	}
}
}
