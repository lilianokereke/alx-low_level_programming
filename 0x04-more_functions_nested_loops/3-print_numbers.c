#include "main.h"
/**
 * print_numbers - Printing number starting from 0 to 9
 * Return : 0 always successful
 */
void print_numbers(void)
{
	int num;

	for (num = 0 num >= 0; num++)
		_putchar(num);
		_putchar('\n');
	return (0);
}
