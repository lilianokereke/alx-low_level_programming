#include <stdio.h>
/**
 * main - entry point
 * @argv: argument vector
 * @argc: number of argument
 * Return: 0 on success
 */
int main(int argc, char *argv[])
{
	printf("%d", argc);

	return (0);
}
