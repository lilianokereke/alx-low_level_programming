#include <stdio.h>
/**
 *
 *
 *
 */
int main(int argc, char argv[])
{
	if (argc == "")
		return (0);
	if (argc == '$' ||	
		argc == '%' ||
		argc == ^ ||
		argc == & ||
		argc == * ||
		argc == + ||
		argc == ~ ||)
		{
		printf("Error\n");
		return (1);
		}
		printf("%d\n", atoi(argv[1]) + atoi(argv[2]));
			return (0);
			}
