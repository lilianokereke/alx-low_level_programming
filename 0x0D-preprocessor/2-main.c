#include <stdio.h>
/**
 * main - prints the name of file in which our code was compiled from followed by a new line
 * Return: 0 on success
 */
int main(void)
{
	printf("%s\n", _FILE_);

	return (0);
}
